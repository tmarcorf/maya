# Maya — dependências de runtime (espelha pyproject.toml).
# Instale uma vez, antes da primeira execução:
#   uv pip install -r requirements.txt
# (ou: uv sync)

httpx>=0.28.1
loguru>=0.7.3
pipecat-ai[kokoro,local,whisper,elevenlabs]>=1.4.0,<2.0
python-dotenv>=1.2.2

# CUDA runtime p/ Whisper em GPU (STT_DEVICE=cuda). O ctranslate2 não embute
# essas libs; o run.sh ajusta o LD_LIBRARY_PATH para o Python enxergá-las.
nvidia-cublas-cu12>=12.4,<13
nvidia-cudnn-cu12>=9.1,<10
nvidia-cuda-runtime-cu12>=12.4,<13
